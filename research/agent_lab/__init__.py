"""Autonomous Alpha Lab — sandbox-only research.

Nothing in this package is imported by production code, the scanner, the
dashboard, MCP, any timer, or any live ledger. It reads cached replay
artifacts only and makes zero provider calls.
"""
