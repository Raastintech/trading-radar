# Weekly Audit — 2026-05-15 (covers 2026-05-08 → 2026-05-15)

Status: **system effectively offline.** Daemon heartbeat is green; trader has placed
no orders since 2026-05-08; position reconciler triggers `MANUAL HALT` every ~5 min.

## Headline numbers

| Metric | Value | Note |
|---|---|---|
| New decision rows | 4 | KVYO/PAHC stuck `orderstatus.new`, PANW filled, VOYA filled. |
| Trades filled & closed | 2 | PANW +14.94% (+$179.52), VOYA -17.34% (-$580.44). Net -$400.92. |
| Council approvals | 45 | 41 of those (HUBS×29, TOST×12) on 2026-05-13 produced **zero** orders. |
| Council vetoes | 270 | 4 tickers carry 99%: CAI 72, TOST 67, OSK 67, HUBS 62. |
| Reconcile-drift audit rows | 3,167 in 2 days | 8 distinct drifts × ~209 cycles/day. No de-dup. |
| Paper signals | 1,863 | **1,859 (99.8%) `governance_blocked`**. |
| Filled rows w/ fill data | 2/88 (~2.3%) | 84 rows have `position_opened=1` but no `fill_price`. |

## Findings (severity-ordered)

### F1 — Stuck reconciler halt (CRITICAL)
- Reconciler reports `broker=3, book=7, matched=1`; the breaker tripped at least
  by 2026-05-14 14:13Z and has retriggered every cycle since. The daemon stays
  halted but reports `is_trading=true` in the heartbeat.
- **DECISIONS_ONLY ghosts (book says open, broker has nothing):**
  W, PH, PEGA, BRO, FLS, CRS. Some are days/weeks old.
- **BROKER_ONLY orphans (broker holds, book missing):** SBAC long 42; KVYO short
  -220 (KVYO's only DB row is `orderstatus.new` — the order **actually filled**
  but the late status update was never captured).

### F2 — `decisions` is "scanner iterations", not trades
- 37 rows for PEGA on a single morning; 22 for CRS; 12 for BRO. Every cycle that
  re-finds an open ticker writes a fresh row. No idempotency in `decision_logger`.
- Downstream corruption: hygiene flags 84 `DECISIONS_OPEN_NO_FILL_PRICE` errors;
  concentration report says PEGA = 48.99% of gross / top-3 = 91.94% — but PEGA
  isn't in the broker. Every operational dashboard built on `decisions` is wrong.

### F3 — Audit query unit error (CORRECTED 2026-05-15)
- Original claim: `paper_signal_outcomes` rows showed PANW LONG +1418.61%,
  INSM SHORT -1001.48%, ZTS SHORT +729.36%.
- Correction: the underlying data is correct (PANW +14.19%, INSM -10.01%,
  ZTS +7.29%). The audit query multiplied an already-PERCENT column by 100.
- Real follow-up: `decisions.pnl_pct` is stored as a FRACTION (0.1419) while
  `paper_signal_outcomes.return_pct` is stored as PERCENT (14.19). Both
  internally consistent, but a footgun for cross-table analytics.
- Fix shipped: UNITS NOTE comments on both DDL sites + the resolver's
  `_raw_return` docstring + a regression test
  `tests/unit/test_outcome_units_convention.py` pinning the convention.

### F4 — Scanner concentration / re-veto loop
- 2026-05-13 SHORT slate is essentially HUBS + TOST. CAI vetoed 72 times in one
  day with 5-min sub-threshold deltas (36.6 → 27.9 → 28.6). No cool-down.
- One-track universe; cycles burned re-evaluating known-no.

### F5 — Council vs submission-gate disagreement
- Submission gate rejected HUBS 41 times: `heat would reach $11,645 (cap: $4,116)`.
- Same HUBS approved by council 29 times. Council sizes trades the gate refuses
  ~3× over. Spend budget approving things that cannot ship.

### F6 — Governance is the universe
- 1,859 / 1,863 paper signals last week ended `governance_blocked`. CAI alone:
  30+ identical rows scored 95, all blocked. Either the strategy fires the same
  name dozens of times (idempotency again) or governance over-rejects. Either
  way validation evidence captured = ~0.

### F7 — Operational blind spots
- `cache/state/broker_positions_snapshot.json` last refreshed 2026-05-12.
- Heartbeat publishes `is_trading=true` while the engine has placed zero orders
  in 7 days.
- Equity hint is `null` in shadow sizing; `shadow_shares` degrades to n/a.
- `orderstatus.new` rows never poll to terminal state — KVYO actually filled on
  the broker side, daemon never noticed.

## What is too broad / naive
- Veto reasons read `profile=default` — per-strategy weight profiles
  (`COUNCIL_PROFILES_ENABLED`) are off. SHORT and SNIPER scored on same weights.
- Single threshold of 50/100 used for every strategy.
- `verdict='RECONCILE_DRIFT'` lives in `veto_log` and dwarfs real council
  verdicts 7:1.
- A single `decisions` row conflates signal + position + fill + audit.
- Halts are binary — a stale drift on KVYO from 2026-05-08 blocks fresh entries
  today.

## What worked
- Live-capital gate held; paper only.
- Council and PositionMonitor exit logic ran cleanly when they got to run.
- Slippage measurement on PANW (-59.5 bps, price improvement) and VOYA (0 bps)
  is sane when the write path captures the fill.
- Phase 1C hygiene report is the tool that surfaced most of this.

## Fix plan (ordered)

1. **Resolve halt + ghost positions** — repair the 8 drift entries from broker
   order history; clear the manual halt; restart daemon.
2. **Idempotency in `decision_logger`** — `log_decision` should upsert against
   `(ticker, strategy, open=1)` rather than insert.
3. **Resolver percent bug** — audit `resolve_tactical_outcomes.py`, write a
   unit-bound test, regenerate `paper_signal_outcomes`.
4. **Stuck-halt alarm** — `is_trading=false` in heartbeat + status sidecar when
   halt persists for N cycles.
5. **Veto cool-down** — park sub-threshold tickers per session unless inputs
   move materially.
6. **Council ↔ submission-gate sizing** — fail-fast at council time when the
   gate cap would refuse.
7. **Refresh broker snapshot in `risk-telemetry`** — read-only `get_positions`;
   fixes Phase 1E coverage.

## Honest summary

We have **N=2 closed trades** in 7 days. Statistically meaningless. The
operational substrate is broken in several places: duplicate decision rows, a
contaminated outcomes ledger, a reconciler that halts the engine without
anyone noticing, a heartbeat that reports green while the trader does nothing.
Validation cannot conclude anything on this telemetry. Fix the plumbing first.
