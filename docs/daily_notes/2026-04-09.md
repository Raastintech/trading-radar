# 2026-04-09

## Implemented
- Added coverage diagnostics in [missed_move_audit.py](/Users/hraastin/Desktop/SniperTradingAI/missed_move_audit.py) and curated overlay support in [universe_snapshot_builder.py](/Users/hraastin/Desktop/SniperTradingAI/universe_snapshot_builder.py).
- Patched curated overlay safety so discovery failure still falls back, ETF/fund symbols do not leak through overlay, and latest snapshot deduping is correct.
- Added learning-flow diagnostics in [learning_data_audit.py](/Users/hraastin/Desktop/SniperTradingAI/learning_data_audit.py).
- Added live stock scorecard in [live_stock_scorecard.py](/Users/hraastin/Desktop/SniperTradingAI/live_stock_scorecard.py).
- Added long miss analysis in [long_reject_leader_audit.py](/Users/hraastin/Desktop/SniperTradingAI/long_reject_leader_audit.py).
- Added deterministic launch env resolution in [launch_env_resolver.py](/Users/hraastin/Desktop/SniperTradingAI/launch_env_resolver.py) and wired it into [start_trader.sh](/Users/hraastin/Desktop/SniperTradingAI/start_trader.sh), [start_monday.sh](/Users/hraastin/Desktop/SniperTradingAI/start_monday.sh), and [start_monday_simple.sh](/Users/hraastin/Desktop/SniperTradingAI/start_monday_simple.sh).
- Fixed the Sniper live scan path in [sniper_scanner_v2.py](/Users/hraastin/Desktop/SniperTradingAI/sniper_scanner_v2.py) so regime-aware scoring receives a valid `regime_context` during paper/live scans.
- Added targeted regression coverage for:
  - recent listings with 20 bars entering the universe
  - Voyager recovery filter admission
  - Sniper relaxed volume ratio admission
  - Sniper elevated-VIX ATR override
  - SHORT `rs_spy=-3` no longer being a fatal reject

## Verified
- `pytest -q test_dynamic_universe_snapshot.py test_missed_move_audit.py` passed after overlay safety fixes.
- `pytest -q test_learning_data_audit.py` passed.
- `pytest -q test_live_stock_scorecard.py` passed.
- `pytest -q test_launch_env_resolver.py test_long_reject_leader_audit.py test_dynamic_universe_snapshot.py test_missed_move_audit.py` passed.
- `pytest -q test_side_tool_integrity.py test_dynamic_universe_snapshot.py test_missed_move_audit.py test_launch_env_resolver.py test_long_reject_leader_audit.py test_learning_data_audit.py test_live_stock_scorecard.py` passed.
- `pytest -q test_sniper_scanner_geometry.py test_dynamic_universe_snapshot.py test_weighted_scan.py` passed.
- Live daemon restart now resolves and runs with `SHORT_LIVE_ENABLED=1`.
- Current readiness remains poor:
  - VOYAGER: `REBUILD`
  - SNIPER: `REBUILD`
  - REMORA: `REBUILD`
  - SHORT: `REBUILD`
  - CONTRARIAN: `REBUILD`

## Open
- Long-side readiness is still not proven. Main repeated blockers from the latest audits:
  - REMORA: `score_below_threshold`
  - VOYAGER: `no_voyager_pathway_qualified`
  - SNIPER: `risk_reward_too_low`
- No April 9 market-session decision rows yet after the short env fix, so there is still no fresh live-session validation today.

## Artifacts
- [logs/missed_move_audit_2026-04-09_amzn-nbis.txt](/Users/hraastin/Desktop/SniperTradingAI/logs/missed_move_audit_2026-04-09_amzn-nbis.txt)
- [logs/learning_data_audit_2026-04-09.txt](/Users/hraastin/Desktop/SniperTradingAI/logs/learning_data_audit_2026-04-09.txt)
- [logs/live_stock_scorecard_2026-04-09.txt](/Users/hraastin/Desktop/SniperTradingAI/logs/live_stock_scorecard_2026-04-09.txt)
- [logs/long_reject_leader_audit_2026-04-09.txt](/Users/hraastin/Desktop/SniperTradingAI/logs/long_reject_leader_audit_2026-04-09.txt)

## Implemented (session 2 — pre-open fixes)
- **Fix 1 — Remora macro-sympathy compatibility**: Made `volume_explosion` non-fatal in
  `REMORA_TIER_1_CRITICAL` ([enhanced_strategy_scoring.py](enhanced_strategy_scoring.py)).
  `catalyst_strength` bool fatal still guards the gate (requires vol≥3x OR macro-sympathy
  vol≥1.5x+price≥3%). Macro-sympathy candidates now score through with 15pts deducted
  instead of being hard-vetoed. Classic high-volume catalyst behavior unchanged.
- **Fix 2 — Voyager institutional_accumulation non-fatal**: Changed `fatal: True` →
  `fatal: False` for `institutional_accumulation` in `VOYAGER_TIER_1_CRITICAL`. Signal
  retains its 10pt weight and recency_decay. Rationale: the signal is a volume-pattern proxy
  (no real 13F feed), and a data gap should not hard-veto an otherwise qualified setup.
- **Fix 3 — Voyager R/R threshold audit**: No code change warranted. Audit across all
  available logs found 1804 pathway failures vs 47 RR failures. Median rejected RR=3.31
  (not clustered near 5.0). The threshold correctly filters stocks whose stop is >10% from
  entry under the fixed 50% target model. Artifact: [logs/voyager_rr_audit_2026-04-09.txt](logs/voyager_rr_audit_2026-04-09.txt).
- **Regression tests**: 13 new tests in `test_fixes_2026_04_09.py` covering all changed paths
  (macro-sympathy non-fatal, score differentiation, classic catalyst pass, Voyager
  inst_accum non-fatal, pathway gate still blocks zero-revenue candidates, RR threshold
  regression). All 13 pass.

## Verified (session 2)
- `pytest -q test_fixes_2026_04_09.py` → 13/13 passed
- `pytest -q test_sniper_scanner_geometry.py test_dynamic_universe_snapshot.py test_weighted_scan.py` → 21/21 passed (no regressions)

## Open (updated after session 2)
- **Pathway qualification remains the dominant Voyager blocker (97.5% of rejections)**.
  Root cause: `yfinance.revenueGrowth` returns None/0 for many candidates in high-VIX
  environments, causing `revenue_growth_yoy=0` which fails all four pathway revenue
  thresholds simultaneously. Fix 2 does not address this (inst_accum never appeared in
  logged Voyager rejections). Proper fix requires: either add data-quality logging so
  genuine zero-revenue stocks are distinguishable from yfinance fetch failures, or add
  a retry/fallback for the fundamental fetch. Logged as structural debt, not a quick fix.
- `guidance_trend` is hardcoded to `'stable'` in `fundamental_data_fetcher.py` — the
  `guidance_improving` check (GROWTH_INFLECTION pathway) can never pass. Low priority
  since GROWTH_INFLECTION only needs one of three improvement signals.
- No April 9 market-session trade decisions yet to validate live trade flow after fixes.

## Artifacts (session 2)
- [logs/voyager_rr_audit_2026-04-09.txt](logs/voyager_rr_audit_2026-04-09.txt)
- [test_fixes_2026_04_09.py](test_fixes_2026_04_09.py)

## Next
- Restart the daemon and observe April 10 market open for Remora and Sniper trade flow
  (most likely to produce decisions now). Voyager requires upstream fundamental data fix
  before meaningful improvement is expected.
- Add verbose per-candidate logging of fundamental field values when a Voyager candidate
  fails all pathways — this distinguishes yfinance data gaps from genuine non-qualification.

## Implemented (session 3 — Voyager wiring fixes)
- **Voyager dynamic-target path actually wired**: propagated `analyst_target_price` through
  the Voyager fundamentals cache in [voyager_production_v2_complete.py](/Users/hraastin/Desktop/SniperTradingAI/voyager_production_v2_complete.py)
  and centralized target selection in a shared helper. The analyst-target R/R logic is now
  live instead of being dropped by the internal cache.
- **Voyager accumulation proxy unified**: added shared regime-calibrated helper methods for
  accumulation thresholds and `inst_buying`, then reused them in both the weighted long-score
  path and `evaluate_opportunity()`. This removes the stale fixed `>=60 / >1.2` logic from the
  evaluation path while preserving the fatal gate in scoring.
- **Voyager revenue-growth fallback covered directly**: added regression coverage proving
  `FundamentalDataFetcher.get_voyager_fundamentals()` derives YoY growth from quarterly
  financials when `yfinance.info['revenueGrowth']` is missing, and preserves `None` when
  growth data is truly unavailable.

## Verified (session 3)
- `pytest -q test_voyager_scoring.py test_side_tool_integrity.py test_fixes_2026_04_09.py test_weighted_scan.py`
  → `36 passed`
- Direct sanity check of `_estimate_long_risk_reward()` with `entry=100`, `stop=92`, and
  `analyst_target_price=240` now returns `17.5:1`, confirming the dynamic target path is active.
- Direct sanity check of `_voyager_inst_buying_signal()` now differs by regime as intended:
  the same `accumulation_score=45 / ratio=1.06` fails in calm tape and passes in fear tape.

## Open (updated after session 3)
- Voyager’s revenue-growth masking bug is materially improved, but live-session validation is
  still pending. We need real April 10 paper decisions to confirm the strategy now converts
  more qualified leaders into ranked candidates.
- `guidance_trend` remains hardcoded to `'stable'` in `fundamental_data_fetcher.py`, so the
  `guidance_improving` branch still cannot contribute to Voyager’s `GROWTH_INFLECTION` pathway.
- We still need better logging around failed Voyager pathways so we can distinguish:
  - genuine low-growth names
  - missing fundamentals
  - names blocked by other pathway checks after revenue growth is recovered

## Next (updated after session 3)
- Run the fixed system through April 10 market open and review actual Voyager/Remora/Sniper
  paper decisions against the missed-move audits.
- Add per-candidate Voyager pathway diagnostics to the live logs so we can see which exact
  pathway checks are still failing after the revenue-growth fallback.
