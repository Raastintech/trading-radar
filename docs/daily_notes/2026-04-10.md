# 2026-04-10

## Implemented
- Reworked the daemon scan architecture in [unified_master_trader_v3.py](/Users/hraastin/Desktop/SniperTradingAI/unified_master_trader_v3.py) so the daemon loop defaults to `5` minutes while each strategy keeps its own cadence:
  - `VOYAGER`: `60m`
  - `SNIPER`: `15m`
  - `REMORA`: `5m`
  - `SHORT`: `60m`
  - `CONTRARIAN`: `60m`
- Extended the REMORA active window in [unified_master_trader_v3.py](/Users/hraastin/Desktop/SniperTradingAI/unified_master_trader_v3.py) from `09:30–10:30 ET` to `09:30–14:30 ET`, keeping the session-time gate but removing the structural “one scan during the whole viable window” problem.
- Replaced Sniper’s earlier `VIX > 20` cliff in [enhanced_strategy_scoring.py](/Users/hraastin/Desktop/SniperTradingAI/enhanced_strategy_scoring.py) with the April 10 calibrated curve:
  - `VIX <= 25`: `0.20`, fatal
  - `VIX 25–30`: `0.13`, non-fatal
  - `VIX > 30`: `0.08`, non-fatal
- Tightened the shared Voyager universe filter in [universe_snapshot_builder.py](/Users/hraastin/Desktop/SniperTradingAI/universe_snapshot_builder.py) to favor pullback-ready leaders instead of fresh-high chase names, using existing `60d` high and medium-trend metrics rather than adding a new long-horizon feature.
- Removed the dead Voyager `guidance_improving` branch from practical inflection-path scoring in [enhanced_strategy_scoring.py](/Users/hraastin/Desktop/SniperTradingAI/enhanced_strategy_scoring.py) and reallocated its weight into the two real inflection signals (`margin_expanding`, `fcf_inflection`) so max-score math stays consistent.
- Removed dead zero-weight bonus signals from the active score configs and mappers in [enhanced_strategy_scoring.py](/Users/hraastin/Desktop/SniperTradingAI/enhanced_strategy_scoring.py) and [strategy_check_mapper.py](/Users/hraastin/Desktop/SniperTradingAI/strategy_check_mapper.py). This was cleanup, not a loosened model; the removed fields were already worth `0`.
- Added per-candidate Voyager pathway failure diagnostics in [voyager_production_v2_complete.py](/Users/hraastin/Desktop/SniperTradingAI/voyager_production_v2_complete.py) so DEBUG logs now show the exact values that failed each of the four Voyager pathways.
- Added paper-only pilot cap defaults in [start_trader.sh](/Users/hraastin/Desktop/SniperTradingAI/start_trader.sh), [start_monday.sh](/Users/hraastin/Desktop/SniperTradingAI/start_monday.sh), and [start_monday_simple.sh](/Users/hraastin/Desktop/SniperTradingAI/start_monday_simple.sh): `PILOT_MAX_NEW_PER_SCAN=3` and `PILOT_MAX_CONCURRENT=6` when `ALPACA_PAPER=1`.
- Reworked live SHORT geometry in [short_scanner_v1.py](/Users/hraastin/Desktop/SniperTradingAI/short_scanner_v1.py) so stops are anchored to completed overhead structure and targets are anchored to the nearest meaningful support below entry. This removes the hidden `4%` stop cap and the formula-only downside targets that distorted April 10’s short book.
- Mirrored the new live SHORT geometry in [eod_scanner_v3.py](/Users/hraastin/Desktop/SniperTradingAI/eod_scanner_v3.py) so offline research and diagnostics stay aligned with the executable path.
- Added short-side revenue-growth fallback in [fundamental_data_fetcher.py](/Users/hraastin/Desktop/SniperTradingAI/fundamental_data_fetcher.py) using the same quarterly-financial TTM YoY logic already used for Voyager. Missing `revenueGrowth` no longer silently becomes `0%` on the short side.
- Tightened SHORT pathway qualification in [enhanced_strategy_scoring.py](/Users/hraastin/Desktop/SniperTradingAI/enhanced_strategy_scoring.py) and [strategy_check_mapper.py](/Users/hraastin/Desktop/SniperTradingAI/strategy_check_mapper.py):
  - high-growth (`revenue_growth_yoy > 15%`) names can no longer qualify through `MARGIN` alone unless there is real secondary deterioration (`guidance_cut`, `estimates_declining`, or `debt_stress`)
  - `VIX < 22` now requires at least `2` SHORT pathways instead of accepting a single-path short in a healthy tape
- Tightened SHORT deterioration attribution in [short_scanner_v1.py](/Users/hraastin/Desktop/SniperTradingAI/short_scanner_v1.py) so `fundamentals_weak` no longer treats valuation richness or unconfirmed high-growth margin compression as actual business deterioration.
- Fixed SHORT reject attribution in [short_scanner_v1.py](/Users/hraastin/Desktop/SniperTradingAI/short_scanner_v1.py) so regime-blocked or guard-blocked pathway failures are recorded as pathway failures instead of being mislabeled as score failures.

## Verified
- `pytest -q test_regime_execution_gates.py test_weighted_scan.py test_dynamic_universe_snapshot.py`
  - `36 passed`
- `pytest -q test_voyager_scoring.py test_portfolio_heat_and_remora.py`
  - `19 passed`
- `pytest -q test_weighted_scan.py test_weighted_score_provenance.py test_side_tool_integrity.py test_voyager_scoring.py`
  - `30 passed`
- `pytest -q test_dynamic_universe_snapshot.py test_regime_execution_gates.py test_portfolio_heat_and_remora.py`
  - `38 passed`
- `bash -n start_trader.sh start_monday.sh start_monday_simple.sh`
  - passed
- Confirmed the new scheduler behavior with direct tests for:
  - per-strategy cadence holding until the interval expires
  - REMORA window active through `14:29 ET` and closed at `14:30 ET`
- Confirmed the calibrated Sniper regime behavior with direct tests at `VIX=22`, `27`, and `32`.
- Confirmed Voyager universe routing still admits pullback-ready names and rejects fresh-high chase profiles.
- Confirmed dead-signal cleanup does not change scores for fully specified candidates because the removed fields were already worth `0`.
- Confirmed Voyager now emits a DEBUG pathway-failure line with concrete check values when no pathway qualifies.
- `pytest -q test_weighted_scan.py test_short_scanner_logic.py`
  - `21 passed`
- `pytest -q test_side_tool_integrity.py`
  - `7 passed`
- `pytest -q test_weighted_score_provenance.py test_phase43.py`
  - `46 passed`
- Confirmed the April 10 pilot-controller diagnosis was partly wrong:
  - the controller was already sorting eligible pilot candidates, not using raw queue order
  - `NET` was not permanently blocked by pilot selection; it later became an executed paper short
  - the real short-side defects were geometry and thesis qualification, not a simple queue-order bug

## Open
- April 10 paper-session evidence improved on the short side, but the two short entries are still open trades, not realized winners yet.
- `guidance_trend` is still effectively dead data for Voyager. The fake branch is no longer affecting qualification or score, but wiring a real source remains follow-on work if we want guidance back in the model.
- The paper pilot cap defaults now exist in the launcher scripts, but they only apply when `ALPACA_PAPER=1` is present in the runtime env.
- Voyager still needs live evidence that the tightened universe plus the earlier fundamental-data fixes convert into better-ranked long candidates instead of only fewer rejects.
- April 10’s pilot ranking still prefers higher `rr` within policy priority, not higher `score`. The post-mortem claim that the system used raw queue order was incorrect, but we may still want a future audit on `rr` vs `score` ordering once more paper short outcomes exist.

## Next
- Watch the next market session closely for:
  - REMORA scans firing throughout the extended window
  - SNIPER producing decisions in `VIX 26–30` tape under the new relaxed ATR curve while remaining strict in ordinary `VIX <= 25` conditions
  - VOYAGER reject mix shifting away from `overextended_for_long_entry`
- On the next short paper session, verify specifically that:
  - high-growth cloud names no longer qualify via `MARGIN` alone in low-VIX tape
  - live SHORT stops are now tied to real prior structure instead of defaulting to an implicit `4%` cap
  - short targets are landing on actual nearby support levels, not deep formula extensions
- If guidance is still worth keeping after live review, wire `guidance_trend` from a real source such as `recommendationTrend` instead of reintroducing a placeholder branch.
- Review the next paper-session decisions against the April 10 reject mix and adjust only the blockers that still show up in real logs.
